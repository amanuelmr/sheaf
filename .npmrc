# React Native's Metro bundler cannot follow pnpm's strict symlinked layout: Expo
# packages import their peers (expo-modules-core, @expo/metro-runtime) expecting a
# flat node_modules, and resolution fails at bundle time rather than at install.
# Expo documents this linker for pnpm monorepos.
node-linker=hoisted
